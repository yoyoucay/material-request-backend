
export enum UserRole {
  ADMIN = 1,
  MANAGER = 2,
  EMPLOYEE = 3,
}

export enum EntityStatus {
  INACTIVE = 0,
  ACTIVE = 1,
}

export enum RequestStatusEnum {
  PENDING = 0,
  APPROVED = 1,
  REJECTED = 2,
  RECEIVED = 3,
}
